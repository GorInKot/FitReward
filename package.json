{
  "name": "fitreward",
  "private": true,
  "workspaces": ["frontend", "backend", "bot"],
  "scripts": {
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "dev:bot": "npm --workspace bot run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend run build && npm --workspace bot run build"
  }
}
